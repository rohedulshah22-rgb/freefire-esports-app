[build]
  command = "pnpm install --frozen-lockfile --prod=false && pnpm exec vite build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
